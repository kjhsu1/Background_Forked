import sys

def greater_than_2(genome_size, n_peaks):
    ls_peaks = []   # final list of all pos of peaks in a each possiblity
    while x != genome_size:
        for pos in range(genome_size):
            

        x += 1
    return ls_peaks

genome_size = int(sys.argv[1])
n_peaks = int(sys.argv[2])
print(greater_than_2(genome_size,n_peaks))
